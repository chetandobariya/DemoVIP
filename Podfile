# Uncomment the next line to define a global platform for your project
 platform :ios, '10.0'

target 'DemoVIP' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

    # Pods for DemoVIP

    # network
    pod 'Alamofire', '~> 5.1'
    pod 'AlamofireImage'

  target 'DemoVIPTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'DemoVIPUITests' do
    # Pods for testing
  end

end
